import type { ConfigType } from "@license-auditor/data";

const config: ConfigType = {
  blacklist: [],
  whitelist: [],
  overrides: {
    assignments: {},
    excluded: [],
  },
};

export default config;

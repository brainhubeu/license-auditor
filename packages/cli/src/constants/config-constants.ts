export const MODULE_NAME = "license-auditor";
export enum ConfigExtension {
  // biome-ignore lint/style/useNamingConvention: pascal case doesn't make sense for this enum
  JS = ".js",
  // biome-ignore lint/style/useNamingConvention: pascal case doesn't make sense for this enum
  CJS = ".cjs",
  // biome-ignore lint/style/useNamingConvention: pascal case doesn't make sense for this enum
  MJS = ".mjs",
  // biome-ignore lint/style/useNamingConvention: pascal case doesn't make sense for this enum
  TS = ".ts",
  // biome-ignore lint/style/useNamingConvention: pascal case doesn't make sense for this enum
  JSON = ".json",
}
